import React from "react";
import styled from "styled-components";

// 리턴 데이터 확인후 타입 지정하기
const Item = () => {
  // 마이페이지 추가 버튼 기능
  /*1.로그인 기능이 없으니 db에 넣지 말고 로컬스토리지 이용
    2.재생목록 추가 버튼시 바로 스토리지 저장
    
  */
  return <OutFrame>Item</OutFrame>;
};

const OutFrame = styled.div``;
export default Item;
